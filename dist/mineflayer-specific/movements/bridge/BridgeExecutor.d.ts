import { Bot } from 'mineflayer';
import { Move } from '../../move';
import { World } from '../../world/worldInterface';
import { MovementExecutor } from '../movementExecutor';
import { MovementOptions } from '../movement';
import * as goals from '../../goals';
import { BridgeConfig } from './BridgeConfig';
import { BuildableMoveExecutor } from '../';
export declare class BridgeExecutor extends MovementExecutor {
    private static readonly MIN_YAW_DELTA_RAD;
    private readonly bridgeConfig;
    private readonly mode;
    private readonly lineTracker;
    private placedThisMove;
    private placementCooldownUntilMs;
    private nextPlacementDelayMs;
    private elevated;
    private elevatedJumpCooldownUntilMs;
    private splicedEndIndex;
    private stallStartMs;
    private _lerpYaw;
    private _lerpPitch;
    constructor(bot: Bot, world: World, settings?: Partial<MovementOptions>, cfg?: Partial<BridgeConfig>);
    static withConfig(cfg?: Partial<BridgeConfig>): BuildableMoveExecutor;
    align(thisMove: Move, tickCount: number, goal: goals.Goal): Promise<boolean>;
    performInit(thisMove: Move, currentIndex: number, path: Move[]): Promise<void>;
    performPerTick(thisMove: Move, tickCount: number, currentIndex: number, path: Move[]): Promise<boolean | number>;
    private _refreshLerp;
    private _isExecutionComplete;
    private _isInWater;
    private _shouldElevate;
    private _applyRotation;
    private _defaultYaw;
    private _defaultPitch;
    private _attemptPlacement;
    private _attemptBreak;
    private _applyMovement;
    private _applySprintState;
    private _applyDirectionalVector;
    private _makeCtx;
    private _countBlocks;
}
